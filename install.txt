'''epson  Epson TM-T82 Printer http://www.amazon.in/Epson-TM-T82-Printer/dp/B01ETKSH92

    pyusb for USB-printers
    Pillow for image printing
    qrcode for the generation of QR-codes
    pyserial for serial printers
    viivakoodi for the generation of barcode



install following steps:-

sudo apt-get install python libusb-1.0-0-dev
git clone https://github.com/walac/pyusb
cd pyusb && sudo python setup.py install
sudo apt-get install python-pip(for python3: wget https://bootstrap.pypa.io/get-pip.py $ sudo python3 get-pip.py ) 
sudo pip install Pillow
sudo pip install pyqrcode


